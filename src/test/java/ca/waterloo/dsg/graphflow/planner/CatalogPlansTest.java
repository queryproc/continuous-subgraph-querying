package ca.waterloo.dsg.graphflow.planner;

import ca.waterloo.dsg.graphflow.planner.Catalog.CatalogPlans;
import ca.waterloo.dsg.graphflow.query.QueryGraph;
import ca.waterloo.dsg.graphflow.query.parser.QueryParser;
import ca.waterloo.dsg.graphflow.storage.KeyStore;
import org.junit.Test;
import org.mockito.Mockito;

import java.util.ArrayList;

public class CatalogPlansTest {
/*
    @Test
    public void test() {
        var keyStore = Mockito.mock(KeyStore.class);
        var queryGraphs = new ArrayList<QueryGraph>();
        queryGraphs.add(QueryParser.parse("(a)->(b),(b)->(c),(a)->(c)", keyStore));
        var catalogPlans = new CatalogPlans(queryGraphs);
        var planCombined = catalogPlans.getPlanCombined();
    }*/
}
